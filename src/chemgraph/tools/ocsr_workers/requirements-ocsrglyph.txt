# OCSRGlyph specialist OCSR model. Python 3.11, CPU torch.
#
#   conda create -p ~/ocsr-glyph python=3.11 -y
#   ~/ocsr-glyph/bin/pip install torch==2.9.1 torchvision==0.24.1 \
#       --index-url https://download.pytorch.org/whl/cpu
#   ~/ocsr-glyph/bin/pip install -r requirements-ocsrglyph.txt
#
# Note the env directory is ~/ocsr-glyph, not ~/ocsr-ocsrglyph.
#
# Weights: model.pth (~374 MB) from HuggingFace EdisonScientific/OCSRGlyph,
# fetched by build_ocsrglyph.sh into ~/ocsr-weights/ocsrglyph/.
#
# Roughly 1.8 GB installed. The lightest and fastest of the four (0.30 s per
# image warm), though the least accurate (76.6% exact).

timm>=1.0
rdkit>=2024.3.1
SmilesPE>=0.0.3
huggingface_hub
